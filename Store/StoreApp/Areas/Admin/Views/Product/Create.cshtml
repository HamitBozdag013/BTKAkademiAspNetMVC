@model ProductDtoForInsertion

<div class="text-center">
    <div class="display-6">Add Products</div>
    <hr />
</div>

<form method="post" asp-action="Create">
    <div class="text-danger" asp-validation-summary="All"></div>
    <div class="form-group my-3">
        @* <label asp-for="CategoryId"></label>
        <select asp-for="CategoryId" class="form-control">
             @foreach(Category category in ViewBag.Categories)
            {
                <option value="@category.CategoryId">@category.CategoryName</option>
            }
        </select> *@
        @* ProductController de SelectList kullandıktan sonra burada da üstteki kod kalabalığından kurtularak alttaki kodlarla da
        aynı işlemi tek satırda <select></select> yapısıyla ve tag helper'larla daha sade bir kod ile yapabiliriz. *@

        <label asp-for="CategoryId"></label>
        <select asp-items="@ViewBag.Categories" asp-for="CategoryId" class="form-control"></select>
    </div>

    <div class="form-group my-3">
        <label asp-for="ProductName"></label>
        <input asp-for="ProductName" class="form-control" />
    </div>
    
    <div class="form-group my-3">
        <label asp-for="Price"></label>
        <input asp-for="Price" class="form-control" />
    </div>
    
    <div class="form-group my-3">
        <div class="btn-group">
            <button class="btn btn-primary" type="submit">
                <i class="fa fa-save"></i>
                Save
            </button>
            <a asp-action="Index" class="btn btn-outline-dark">
                <i class="fa fa-list"></i>
                Product
            </a>    
        </div>
    </div>
</form>